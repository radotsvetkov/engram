<!-- Thanks for the contribution! Keep it focused — one logical change per PR. -->

## What this does

<!-- A short description of the change and the problem it solves. -->

## Why

<!-- The motivation / linked issue, if any. Closes #... -->

## Checklist

- [ ] `cargo fmt --all` is clean
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [ ] `cargo test --workspace` passes
- [ ] `cargo run -p engram-eval` passes (and I added/updated a case if agent behaviour changed)
- [ ] Docs updated where behaviour or configuration changed
